#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {
	int x, y, z;
	int sum;
	printf("ÊäÈëÈı¸öÊä x, y, z:");
	scanf("%d, %d, %d", &x, &y, &z);
	sum = x+y+z;
	printf(" sum=%d\n", sum);
	return 0;
}